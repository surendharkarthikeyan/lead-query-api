spring:
  application:
    name: lead-query-api

  datasource:
    url: jdbc:postgresql://localhost:5432/lead_query_db
    username: postgres
    password: Mega!2005
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml